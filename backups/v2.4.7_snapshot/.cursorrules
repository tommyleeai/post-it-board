# 白板專案 (Post It) — 開發規範與 AI 指引

## 部署與版本更新規則（必須嚴格遵守）

當使用者要求「上傳」、「部署」或「更新版號」時，**絕對禁止**手動逐一修改 `package.json`、`index.html` 或 `CHANGELOG.md`。

你**必須**直接呼叫專案內的自動化部署腳本，讓系統一條龍處理所有檔案的版號同步與 Git 動作。

### 執行指令
在專案根目錄下執行：
```bash
node scripts/deploy.js [patch|minor|major|特定版號] "更新說明"
```

**範例：**
- `node scripts/deploy.js patch "修復了白板卡片層級問題"`
- `node scripts/deploy.js 2.2.0 "大改版：新增多白板功能"`

### 腳本自動處理的範圍（請勿手動介入）：
1. 同步更新 `package.json` 的 `version`。
2. 同步更新 `js/changelog.js` 裡的 `CURRENT_VERSION`。
3. 自動修改 `index.html` 的 UI 顯示版號，並替換 CSS 快取參數（`?v=...`）。
4. 將更新說明自動寫入 `docs/CHANGELOG.md`。
5. 建立 `backups/vX.X_snapshot/` 快照備份。
6. 自動執行 `git add .`、`git commit` 與 `git push`。
